#unpacking in tuples
coordinates = (1,2,3)
x, y, z = coordinates #we can assign items like this which is called unpacking
print(y)


#unpacking in lists
coordinates = [1,2,3]
x, y, z = coordinates #we can assign items like this which is called unpacking
print(y)